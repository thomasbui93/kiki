import React from 'react'
import { RouterProps } from 'react-router'

export const ProjectPage = (props: RouterProps) => {
  return (
    <React.Fragment>
      <h1>Project Loading ...</h1>
    </React.Fragment>
  )
}
